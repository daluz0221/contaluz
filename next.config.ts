import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {

    domains: [
      "images.unsplash.com",
      "static.vecteezy.com",
      "img.freepik.com"
    ]
  }
};

export default nextConfig;
